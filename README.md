# StateManagerTimestamped

A TypeScript-based state management library with timestamping, versioning, and navigation. Includes unit tests with Jest.

## Installation

Install dependencies:

```bash
npm install
